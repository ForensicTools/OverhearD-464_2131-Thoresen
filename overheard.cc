#include <string>
#include <iostream>
#include <thread>
#include <cstdlib>

using namespace std;


int main(int argc, char ** argv)
{ 
    //thread coreThread(testTask, "");
    //coreThread.join();
    cout << "Currently Programming" << endl;
    return 0;
}
